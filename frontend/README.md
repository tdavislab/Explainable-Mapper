# Frontend notes
#
# Full setup and run instructions for this project are in the repository root:
#   ../README.md
#   ../docs/REQUIREMENTS.md
#
# Quick start (from this folder):
#   npm install
#   npm start
#
# The CRA app proxies /api to the Flask backend on port 5005
# (see src/setupProxy.js). Start the backend first:
#   cd ../backend && source venv/bin/activate && python app.py
